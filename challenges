<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChromaBurst Challenge</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        .logo {
            font-size: 64px;
            margin-bottom: 20px;
        }
        h1 {
            font-size: 32px;
            margin: 20px 0;
        }
        .spinner {
            width: 50px;
            height: 50px;
            border: 5px solid rgba(255,255,255,0.3);
            border-top-color: white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 30px auto;
        }
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        .button {
            background: white;
            color: #667eea;
            padding: 15px 40px;
            border-radius: 30px;
            text-decoration: none;
            font-size: 18px;
            font-weight: bold;
            margin-top: 30px;
            display: inline-block;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
    <div class="logo">🎮</div>
    <h1>ChromaBurst Challenge</h1>
    <p>Redirecting to app...</p>
    <div class="spinner"></div>
    <a href="https://play.google.com/store/apps/details?id=com.chromaburst.game" class="button">
        Download from Play Store
    </a>

    <script>
        // Challenge ID'yi URL'den al
        const urlParams = new URLSearchParams(window.location.search);
        const challengeId = urlParams.get('id');

        if (challengeId) {
            // Deep link'e yönlendir (uygulama yüklüyse açılır)
            const deepLink = `chromaburst://challenge?id=${challengeId}`;
            window.location.href = deepLink;

            // 2 saniye sonra Play Store'a yönlendir (uygulama yüklü değilse)
            setTimeout(() => {
                window.location.href = `https://play.google.com/store/apps/details?id=com.chromaburst.game`;
            }, 2000);
        } else {
            // Challenge ID yoksa direkt Play Store
            window.location.href = 'https://play.google.com/store/apps/details?id=com.chromaburst.game';
        }
    </script>
</body>
</html>
